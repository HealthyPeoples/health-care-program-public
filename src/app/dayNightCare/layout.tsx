import { NursingHome } from '../../component/nursing-home';

export default function DayNightCareLayout({ children }: { children: React.ReactNode }) {
  return <NursingHome>{children}</NursingHome>;
}

