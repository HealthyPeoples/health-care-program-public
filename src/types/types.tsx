/**
 * @file 공통 타입 — types.tsx
 *
 * @description
 * 프로젝트 전역 TypeScript 타입 정의입니다.
 *
 * @module types/types
 */
import { ReactNode } from 'react'

export interface ReactNodeProps {
  children: ReactNode
}
