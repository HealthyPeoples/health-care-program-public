/**
 * @file 일 수급자급여실적 View 스텁
 *
 * @description
 * 예전 분리용 플레이스홀더입니다. 실제 UI는
 * {@link ./DailyBeneficiaryPerformance} 기본 export를 사용합니다.
 *
 * @remarks
 * `src/app/nursingHome/daily-beneficiary-performance/page.tsx`는
 * 이 파일이 아니라 `DailyBeneficiaryPerformance.tsx`를 import합니다.
 * 신규 작업 시 이 스텁을 확장하지 말고 메인 컴포넌트를 수정하세요.
 */
"use client";
import React from 'react';

/**
 * 미사용 스텁 컴포넌트. 프로덕션 라우트에서 렌더되지 않습니다.
 * @returns 플레이스홀더 마크업
 */
export default function DailyBeneficiaryPerformanceView() {
	return (
		<div className="min-h-screen w-full max-w-full min-w-0 overflow-x-hidden bg-white text-black">
			DailyBeneficiaryPerformanceView
		</div>
    );
}
